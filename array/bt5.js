let object = [1,2,3,'a','b','c',null,undefined,true];
console.log("trước khi đảo");
for (const iterator of object) {
    console.log(iterator);
}
console.log("sau khi đảo");
let lenght = object.length - 1;
for (let index in numberList) {
    temp = numberList[i];
    numberList[i] = numberList      
}
for (const iterator of object) {
    console.log(iterator);
}

